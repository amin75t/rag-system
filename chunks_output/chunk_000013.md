# Chunk 13

trace هم بدین نام ایجاد و observation ذکر شده ذیل این trace قرار می‌گیرد.
همانطور که در جدول مشخص است، انتظار می‌رود مابقی observationها ذیل observation و trace به نام process_user_query قرار بگیرند.
همچنین کاربر (user) مربوط به هر trace هم باید در مشخصات آن ثبت شده باشد؛ نحوه‌ی تعیین کاربر و موارد دیگر مربوط به trace در سند راهنمای Langfuse آورده شده است.
در زیر لیست تمامی observationهای مورد انتظار به همراه ورودی و خروجی‌های مربوطه ذکر شده است: 
جدول 1
*بنابر نوع پیاده‌سازی ممکن است از یک یا چند پایگاه داده استفاده شود. برای مثال یکی برای vector search و یکی برای hybrid search در اینجا دو متد primary و secondary برای سرچ وجود دارد که اگر از یک نوع استفاده شده فقط متد اول رصد می‌شود و اگر از شیوه‌ی دومی هم برای سرچ بر روی پایگاه داده استفاده شده است متد secondary هم باید تعریف و رصد شود.

---

