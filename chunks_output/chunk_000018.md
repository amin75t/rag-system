# Chunk 18

est_trace")
    test_child_observe(name)
    return "Hello" + name

@observe
def test_child_observe(name: str):
    return "Bye" + name

test_parent_observe("World!") |
|---|


**Table 12**

| client.update_current_trace(name="test_trace", user_id="1234", session_id="110", tags=["test", "monitoring"]) |
|---|


**Table 13**

| import os
import asyncio
from langfuse.openai import AsyncOpenAI
from langfuse import observe

client = AsyncOpenAI(api_key=os.getenv("OPENAI_API_KEY"))

@observe
async def call_llm():
    return await client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {"role": "system", "content": "You are a helpful assistant."},
            {"role": "user", "content": "Hello"},
        ],
    )

asyncio.

---

