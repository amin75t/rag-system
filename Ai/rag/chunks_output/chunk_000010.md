# Chunk 10

اخل یک observe انجام شود (برای مثال بروزرسانی نام trace)، در نهایت بر روی خروجی نهایی در Langfuse اعمال خواهد شد.
تصویر 10
همانطور که در شکل بالا (تصویر 10) مشخص است نام trace به test_trace تغییر کرد.
موارد زیادی در update_trace قابل تغییر و تعیین است به مثال زیر توجه کنید:
کد 12
## OpenAI Generation
در SDK مربوط به Langfuse قابلیتی ارائه شده است که می‌توان به جای استفاده از کلاینت رسمی openai از کلاینت openai ارائه شده توسط Langfuse استفاده کرد.
توجه شود لزوما استفاده از openai مطرح نیست، بلکه منظور استفاده از کلاینت openai برای تمامی مدل‌های سازگار با این کلاینت است.
کد 13
با استفاده از کلاینت langfuse.openai پارامترهایی از جمله هزینه مدل و … داخل observation مربوطه از جنس Generation قرار می‌گیرد (تصویر 11).
تصویر 11
در صورت عدم نیاز به استفاده از کلاینت langfuse.openai نیاز است که به صورت دستی موارد بالا تنظیم شوند.
برای اطلاعات بیشتر می‌توانید به سند زیر مراجعه کنید:
https://langfuse.

---

